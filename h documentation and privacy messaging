warning: in the working copy of 'README.md', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/components/UploadLanding.jsx', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/README.md b/README.md[m
[1mindex 2970d72..91ea846 100644[m
[1m--- a/README.md[m
[1m+++ b/README.md[m
[36m@@ -26,7 +26,7 @@[m [mStudents spend significant time turning lecture materials into study guides. Tra[m
 2. **Analysis**: Server extracts raw text and parses page/slide structure:[m
    - **PDF**: Processed using `pdf-parse` v2 on server-side in-memory buffer.[m
    - **DOCX**: Extracted using `mammoth` (with `officeparser` fallback) to parse raw formatted document text.[m
[31m-   - **PPTX**: Extracted using `officeparser` to parse presentation slide contents and text frames.[m
[32m+[m[32m   - **PPTX**: Extracted directly from the presentation ZIP structure using `adm-zip` to parse slide XML files (`ppt/slides/slide*.xml`) and `<a:t>` text elements (with `officeparser` fallback).[m
 3. **AI Generation**: Gemini 3.6 Flash analyzes the normalized text under strict grounding constraints.[m
 4. **Study Experience**: Student views structured topic notes, key concept definitions, "Key Takeaway" callout boxes, and switches seamlessly to a 5-question active recall practice quiz.[m
 5. **Export**: Student exports the complete revision pack as Markdown (`.md`), copies plain text, or prints/saves as PDF.[m
[36m@@ -65,7 +65,7 @@[m [mStudents spend significant time turning lecture materials into study guides. Tra[m
 ## 🛠️ Technology Stack[m
 [m
 - **Frontend**: React 19, Vite, Tailwind CSS (CDN/Custom System), Lucide Icons, Canvas Confetti.[m
[31m-- **Backend**: Node.js, Express, Multer, `pdf-parse` v2, `mammoth`, `officeparser`.[m
[32m+[m[32m- **Backend**: Node.js, Express, Multer, `pdf-parse` v2, `mammoth`, `adm-zip`, `officeparser`.[m
 - **AI Engine**: Google Gen AI SDK (`@google/genai`), Model: `gemini-3.6-flash`.[m
 [m
 ---[m
[36m@@ -121,6 +121,6 @@[m [mThe output will be generated in the `dist/` folder.[m
 [m
 ## 📌 Assumptions & Limitations[m
 [m
[31m-1. **Extractable Text**: Reviso processes digital PDF, DOCX, and PPTX files containing extractable text layers. Image-only scanned files without OCR text will prompt an error asking for a text-based document.[m
[32m+[m[32m1. **Extractable Text**: Reviso processes digital PDF, DOCX, and PPTX files containing extractable text layers. Image-only scanned documents without an extractable text layer will prompt an error asking for a text-based document.[m
 2. **Grounding Scope**: AI generation is strictly bound to the uploaded document text (45,000 characters input limit per request) to prevent hallucinated concepts.[m
 3. **Single-Purpose Focus**: Reviso intentionally omits calendars, generic chat, flashcards, and task managers to preserve single-flow revision focus.[m
\ No newline at end of file[m
[1mdiff --git a/src/components/UploadLanding.jsx b/src/components/UploadLanding.jsx[m
[1mindex 8bc17cc..ae3246e 100644[m
[1m--- a/src/components/UploadLanding.jsx[m
[1m+++ b/src/components/UploadLanding.jsx[m
[36m@@ -164,7 +164,7 @@[m [mexport default function UploadLanding({ onUploadSubmit, onSelectSample }) {[m
                   <span>•</span>[m
                   <span>Max 50MB per file</span>[m
                   <span>•</span>[m
[31m-                  <span>Hand-written OCR ready</span>[m
[32m+[m[32m                  <span>Text-based documents supported</span>[m
                 </div>[m
               </div>[m
             ) : ([m
[36m@@ -209,7 +209,7 @@[m [mexport default function UploadLanding({ onUploadSubmit, onSelectSample }) {[m
           <div className="mt-8 flex flex-col sm:flex-row items-center justify-between gap-4">[m
             <div className="flex items-center gap-2 text-xs text-slate-500">[m
               <Lock className="w-4 h-4 text-emerald-600 shrink-0" />[m
[31m-              <span>Your uploaded course materials are encrypted and never shared.</span>[m
[32m+[m[32m              <span>Your uploaded course materials are processed server-side and are not stored.</span>[m
             </div>[m
 [m
             <button[m
